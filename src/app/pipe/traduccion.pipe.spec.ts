import { TraduccionPipe } from './traduccion.pipe';

describe('TraduccionPipe', () => {
  it('create an instance', () => {
    const pipe = new TraduccionPipe();
    expect(pipe).toBeTruthy();
  });
});
