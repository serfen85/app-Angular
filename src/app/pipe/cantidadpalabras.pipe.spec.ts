import { CantidadpalabrasPipe } from './cantidadpalabras.pipe';

describe('CantidadpalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadpalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
